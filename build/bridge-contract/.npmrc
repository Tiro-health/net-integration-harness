# Scope mapping only — no _authToken here, matching build/web-sdk/.npmrc: a
# literal ${NODE_AUTH_TOKEN} that a reader cannot resolve overrides supplied
# credentials (this is what broke Dependabot). CI gets auth from
# actions/setup-node; local dev from ~/.npmrc (see README.md).
@tiro-health:registry=https://npm.pkg.github.com
