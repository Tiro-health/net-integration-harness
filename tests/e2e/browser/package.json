{
  "name": "e2e-browser",
  "private": true,
  "type": "module",
  "description": "GH-26 layer 1: the real <tiro-form-filler> driven by the real bridge against a real SDC server. Complements tests/bridge (stub element) and build/bridge-contract (types only).",
  "scripts": {
    "test": "node --test flow.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  }
}
